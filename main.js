// this is the main interface file which should be importing stuff from modules should be served on the CDN
import { PlayerHealth } from "./components/networked-raytrace-v5/main"

export default PlayerHealth